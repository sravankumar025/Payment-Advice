const PrintableInvoice = ({ data }) => {
  return (
    <div className="p-8 max-w-4xl mx-auto bg-white text-black font-sans">
      {/* 1. Document Header */}
      <div className="flex justify-between items-center border-b pb-4 mb-6">
        <div>
          <h1 className="text-2xl font-bold uppercase tracking-wide">Payment Advice</h1>
          <p className="text-sm text-gray-500">Shalimar Roller Flour Mill</p>
        </div>
        <div className="text-right text-sm">
          <p><strong>Date:</strong> {data.date}</p>
          <p><strong>Ref No:</strong> {data.refNo}</p>
        </div>
      </div>

      {/* 2. Customer / Transaction Details Grid */}
      <div className="grid grid-cols-2 gap-4 text-sm mb-6 bg-slate-50 p-4 rounded">
        <div>
          <p><strong>Party Name:</strong> {data.partyName}</p>
          <p><strong>Location:</strong> {data.location}</p>
        </div>
        <div>
          <p><strong>A/c No:</strong> {data.accountNo}</p>
          <p><strong>Broker:</strong> {data.broker}</p>
        </div>
      </div>

      {/* 3. Items Table */}
      <table className="w-full text-left text-sm border-collapse mb-6">
        <thead>
          <tr className="border-b bg-gray-100">
            <th className="py-2 px-3">#</th>
            <th className="py-2 px-3">Qty</th>
            <th className="py-2 px-3">Rate</th>
            <th className="py-2 px-3 text-right">Amount</th>
          </tr>
        </thead>
        <tbody>
          {data.items.map((item, index) => (
            <tr key={index} className="border-b">
              <td className="py-2 px-3">{index + 1}</td>
              <td className="py-2 px-3">{item.qty}</td>
              <td className="py-2 px-3">{item.rate}</td>
              <td className="py-2 px-3 text-right">{item.amount}</td>
            </tr>
          ))}
        </tbody>
      </table>

      {/* 4. Totals & Deductions */}
      <div className="flex justify-end text-sm">
        <div className="w-1/2 space-y-2 border-t pt-2">
          <div className="flex justify-between font-semibold">
            <span>Total Amount:</span>
            <span>{data.totalAmount}</span>
          </div>
        </div>
      </div>
    </div>
  );
};
